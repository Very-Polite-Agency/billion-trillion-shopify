{%- liquid

  assign section_name = 'featured-product'
  assign section_classes = section_name
  assign section_id = section_name | append: '--' | append: section.id

  assign aos_id = section_id | append: '--aos'
  assign aos_delay = 250
  assign aos_delay_increment = 250

  assign container = section.settings.container | default: 'container'

  assign section_product = section.settings.product | defaul: false
  assign section_anchor_id = section.settings.anchor_id | default: ''

  assign section_heading = section_product.title | default: ''
  assign section_image = section_product.featured_image | default: false
  assign section_message = section_product.description | default: ''

-%}

{%- style -%}

  {%
    render 'element-styles',
      border_bottom: section.settings.border_bottom,
      border_top: section.settings.border_top,
      id: section_id
  %}

{%- endstyle -%}

<div class="{{ section_classes }}" id="{{ section_id }}">
  <div class="{{ section_name }}__grid grid" id="{{ aos_id }}" role="list">
    {%- for block in section.blocks -%}
      <div class="{{ section_name }}__item {{ block.type }}" role="listitem">
        {%- case block.type -%}

          {%- when 'content' -%}

            {%- liquid
              assign block_id = block.id | prepend: 'block-id--'
              assign section_heading = block.settings.heading | default: section_heading
              assign section_message = block.settings.message | default: section_message
            -%}

            {%
              render 'element-styles',
                background: block.settings.background
                id: block_id
            %}

            <div class="{{ section_name }}__content" id="{{ block_id }}">
              {% if section_heading != blank %}
                <strong>{{ section_heading }}</strong>
              {% endif %}
              {% if section_message != blank %}
                <div>{{ section_message }}</div>
              {% endif %}
            </div>

          {%- when 'image' -%}

            {%- liquid
              assign section_badge = block.settings.badge | default: 'none'
              assign section_image = block.settings.image | default: section_image
            -%}

            {% if section_image %}
              <div class="{{ section_name }}__image">
                {% render 'lazyload-image', image: section_image %}
                {% unless 'none' == section_badge %}
                  <div class="{{ section_name }}__image-badge">
                    {% capture badge_type %}badge.{{ section_badge }}{% endcapture %}
                    {% render 'svg', type: badge_type %}
                  </div>
                {% endunless %}
              </div>
            {% endif %}

        {%- endcase -%}
      </div>
    {%- endfor -%}
  </div>
</div>

{% schema %}
{
  "name": "Featured Product",
  "tag": "section",
  "class": "section section--featured-product",
  "limit": 1,
  "settings": [
    {
      "content": "Content",
      "type": "header"
    },
    {
      "label": "Product",
      "type": "product",
      "id": "product"
    },
    {
      "content": "Settings",
      "type": "header"
    },
    {
      "content": "Anchor",
      "type": "header"
    },
    {
      "label": "Anchor ID",
      "info": "Optionally include a unique ID so that sections within the page can be linked to. Anchor IDs must be all lowercase, unique and replaced empty spaces with hyphens. For example: my-great-section-anchor",
      "type": "text",
      "id": "anchor_id"
    },
    {
      "content": "Border",
      "type": "header"
    },
    {
      "label": "Top Border",
      "type": "checkbox",
      "default": false,
      "id": "border_top"
    },
    {
      "label": "Bottom Border",
      "type": "checkbox",
      "default": false,
      "id": "border_bottom"
    },
    {
      "content": "Layout",
      "type": "header"
    },
    {
      "label": "Layout (Desktop)",
      "type": "select",
      "options": [
        { "label": "Image Left", "value": "image-left" },
        { "label": "Image Right", "value": "image-right" }
      ],
      "default": "image-left",
      "id": "layout"
    }

  ],
  "blocks": [
    {
      "name": "Content",
      "type": "content",
      "limit": 1,
      "settings": [
        {
          "content": "Content",
          "type": "header"
        },
        {
          "label": "Heading",
          "type": "text",
          "id": "heading"
        },
        {
          "label": "Message",
          "type": "richtext",
          "id": "message"
        },
        {
          "content": "Settings",
          "type": "header"
        },
        {
          "label": "Background",
          "type": "select",
          "options": [
            { "label": "Lemon", "value": "lemon" },
            { "label": "Lime", "value": "lime" },
            { "label": "Mango", "value": "mango" },
            { "label": "None", "value": "none" },
            { "label": "Orange", "value": "orange" },
            { "label": "Strawberry", "value": "strawberry" }
          ],
          "default": "none",
          "id": "background"
        }
      ]
    },
    {
      "name": "Image",
      "type": "image",
      "limit": 1,
      "settings": [
        {
          "content": "Content",
          "type": "header"
        },
        {
          "label": "Image",
          "type": "image_picker",
          "id": "image"
        },
        {
          "label": "Badge",
          "type": "select",
          "options": [
            { "label": "Hot", "value": "hot" },
            { "label": "None", "value": "none" }
          ],
          "default": "none",
          "id": "badge"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Featured Product"
    }
  ]
}
{% endschema %}
