{%- liquid

  assign section_name = 'featured-product'
  assign section_classes = section_name
  assign section_id = section_name | append: '--' | append: section.id

  assign aos_id = section_id | append: '--aos'
  assign aos_delay = 250
  assign aos_delay_increment = 250

  assign container = section.settings.container | default: 'container'

  assign section_product = section.settings.product | defaul: false
  assign section_anchor_id = section.settings.anchor_id | default: ''

  assign section_heading = section_product.title | default: ''
  assign section_image = section_product.featured_image | default: false
  assign section_message = section_product.description | default: ''
  assign section_link = section_product.url | default: ''

-%}

{%- style -%}

  {%
    render 'element-styles',
      border_bottom: section.settings.border_bottom,
      border_top: section.settings.border_top,
      id: section_id
  %}

{%- endstyle -%}

<div class="{{ section_classes }}" id="{{ section_id }}">

  {% capture snippet_classes %}{{ section_name }}__main fifty-fifty{% endcapture %}
  {%
    render 'fifty-fifty',
      classes: snippet_classes
      heading: section_heading,
      id: section_id | append: '--fify-fifty',
      image: section_image,
      message: section_message,
      link: section_link
  %}

</div>

{% schema %}
{
  "name": "Featured Product",
  "tag": "section",
  "class": "section section--featured-product",
  "settings": [
    {
      "content": "Content",
      "type": "header"
    },
     {
      "content": "By default, the heading, image and body copy will automatically be pulled from the selected product. To customize, and override this automation, update the corresponding fields within this section's blocks.",
      "type": "paragraph"
    },
    {
      "label": "Product",
      "type": "product",
      "id": "product"
    },
    {
      "content": "Settings",
      "type": "header"
    },
    {
      "content": "Anchor",
      "type": "header"
    },
    {
      "label": "Anchor ID",
      "info": "Optionally include a unique ID so that sections within the page can be linked to. Anchor IDs must be all lowercase, unique and replaced empty spaces with hyphens. For example: my-great-section-anchor",
      "type": "text",
      "id": "anchor_id"
    },
    {
      "content": "Border",
      "type": "header"
    },
    {
      "label": "Top Border",
      "type": "checkbox",
      "default": false,
      "id": "border_top"
    },
    {
      "label": "Bottom Border",
      "type": "checkbox",
      "default": false,
      "id": "border_bottom"
    }
  ],
  "blocks": [
    {
      "name": "Content",
      "type": "content",
      "limit": 1,
      "settings": [
        {
          "content": "Content",
          "type": "header"
        },
        {
          "label": "Heading",
          "info": "Optionally override the product title",
          "type": "text",
          "id": "heading"
        },
        {
          "label": "Message",
          "info": "Optionally override the product description",
          "type": "richtext",
          "id": "message"
        },
        {
          "content": "Settings",
          "type": "header"
        },
        {
          "label": "Background",
          "type": "select",
          "options": [
            { "label": "Lemon", "value": "lemon" },
            { "label": "Lime", "value": "lime" },
            { "label": "Mango", "value": "mango" },
            { "label": "None", "value": "none" },
            { "label": "Orange", "value": "orange" },
            { "label": "Strawberry", "value": "strawberry" }
          ],
          "default": "none",
          "id": "background"
        }
      ]
    },
    {
      "name": "Image",
      "type": "image",
      "limit": 1,
      "settings": [
        {
          "content": "Content",
          "type": "header"
        },
        {
          "label": "Image",
          "info": "Optionally override the product's featured image",
          "type": "image_picker",
          "id": "image"
        },
        {
          "label": "Badge",
          "type": "select",
          "options": [
            { "label": "Hot", "value": "hot" },
            { "label": "None", "value": "none" }
          ],
          "default": "none",
          "id": "badge"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Featured Product",
      "blocks": [
        { "type": "image" },
        { "type": "content" }
      ]
    }
  ]
}
{% endschema %}
